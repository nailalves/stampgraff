<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "App_Name" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "App_Name" %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
  </head>
  <body>
    <header class="navbar navbar-fixed-top">
      <nav class="navbar-inner">
        <div class="container">
          <%= render 'layouts/navigation' %>
        </div>
      </nav>
    </header>
			<div id="main" role="main">
				<div class="container-fluid">
					<% if user_signed_in? %>
						<div class="span2">
							<div class="row-fluid">
								<ul class="nav nav-list">
									<li class="nav-header">MENU</li>
									<li><a href="/ordems/new">Criar ordem de serviço</a></li>
									<li><a href="/produtos/new">Criar produto</a></li>
									<li><a href="/clientes/new">Criar novo cliente</a></li>
								</ul>
							</div>
						</div>
					<%end%>
					<% if user_signed_in? %>
						<div class="content span10">
					<%else%>
						<div class="content span12">
					<%end%>
						<div>
							 <%= render 'layouts/messages' %>
							 <%= yield %>
						</div>
						<footer>
						</footer>
					</div>
				</div> <!--! end of .container -->
			</div> <!--! end of #main -->
  </body>
</html>
