<div class="inner_content">
<h1 class="page-header">Listing produtos</h1>

<table class="table table-striped">
  <tr>
    <th>Nome</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @produtos.each do |produto| %>
  <tr>
    <td><%= produto.name %></td>
    <td><%= link_to 'Mostrar', produto %></td>
    <td><%= link_to 'Editar', edit_produto_path(produto) %></td>
    <td><%= link_to 'Deletar', produto, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />
</div>
<div class="form-actions">
	<%= link_to 'Novo Produto', new_produto_path, :class => 'btn btn-primary' %>
</div>

